type RowObj = {
	parameter: [string];
	original: number;
	difference: number;
	lock: boolean;
};

const tableDataCheck: RowObj[] = [
	{
		parameter: [ 'Horizon UI PRO'],
		original: 2458,
		difference: 17.5,
		lock: false
	}
];

export default tableDataCheck;
